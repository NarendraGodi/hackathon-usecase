## Instructions

- make sure to run the application to download the dependencies
- Only then build Dockerfile
- express module need to be installed as a pre-requisite
- npm install express
- service is exposed on port 3000